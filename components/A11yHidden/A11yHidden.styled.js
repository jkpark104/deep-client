import styled from '@emotion/styled';

const A11yHidden = styled.span`
  overflow: hidden;
  position: absolute;
  clip: rect(1px 1px 1px 1px);
  width: 1px;
  height: 1px;
  margin: -1px;
  white-space: nowrap;
`;

export default {
  A11yHidden,
};
